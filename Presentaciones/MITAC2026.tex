\documentclass[compress,12pt]{beamer}

\usetheme{Arguelles}

\usepackage{graphicx}
\usepackage{caption}
\usepackage[spanish,es-noshorthands]{babel}
\usepackage[pages=some]{background}

\usepackage{tikz}
\usepackage{tikz-cd}
\usetikzlibrary{
  babel,
  arrows.meta,
  calc,
  positioning,
  overlay-beamer-styles,
  shadows.blur
}
\usetikzlibrary{decorations.pathmorphing}

\usepackage{amsmath,amssymb,latexsym,amscd} 
\usepackage[all,cmtip]{xy}
\usepackage{fancyhdr}
\usepackage{mathalfa}
\usepackage{mathrsfs}
\usepackage{hyperref}
\usepackage{ragged2e}
\usepackage{wasysym}
\usepackage[authoryear]{natbib}

% Colores para la diapositiva Top/Frm
\definecolor{TopBlue}{RGB}{14,73,160}
\definecolor{FrmGreen}{RGB}{15,120,45}
\definecolor{TopLight}{RGB}{235,242,252}
\definecolor{FrmLight}{RGB}{235,248,238}

\pagenumbering{arabic}

\DeclareMathOperator{\op}{op}
\DeclareMathOperator{\pt}{pt}
\DeclareMathOperator{\tp}{tp}
\DeclareMathOperator{\Sp}{sp}
\DeclareMathOperator{\SP}{Sp}
\DeclareMathOperator{\Sob}{Sob}
\DeclareMathOperator{\spec}{spec}
\DeclareMathOperator{\Fit}{Fit}
\DeclareMathOperator{\Pth}{P}
\DeclareMathOperator{\Vrm}{Vrm}
\DeclareMathOperator{\Frm}{Frm}
\DeclareMathOperator{\Top}{Top}
\DeclareMathOperator{\Ord}{Ord}
\DeclareMathOperator{\Obj}{Obj}
\DeclareMathOperator{\Hom}{Hom}

\title{Marcos apilados y fuertemente apilados}
%\subtitle{}
\event{MITAC 2026, BUAP}
\date{4 de septiembre de 2026}
\author{Juan Carlos Monter Cortés \\ Dr. Luis Ángel Zaldívar Corichi}
\institute{Universidad de Guadalajara}
\email{juan.monter2902@alumnos.udg.mx}

%\homepage{www.mywebsite.com}
\github{JCmonter}

\begin{document}


%\frame[plain]{\titlepage}

\begin{frame}[plain]
\titlepage

\vfill
\begin{center}
    \textcolor{white}{\scriptsize Investigación apoyada por SECIHTI-PROYECTO CBF2023-2024-2630}
\end{center}
\end{frame}

\begin{frame}{Contenido}
\tableofcontents
\end{frame}

\section{Teoría preliminar}

\begin{frame}[fragile]{Espacios y marcos}

%------------------------------------------------
% Parte superior
%------------------------------------------------
\begin{columns}[T,onlytextwidth]

    \begin{column}{0.48\textwidth}
        \[
        \begin{tikzcd}[column sep=large, row sep=large]
            \Frm & \SP \\
            \Top & \Sob
            \arrow["\pt", shift left=3,
                from=1-1, to=2-1]
            \arrow[hook', from=1-2, to=1-1]
            \arrow[shift left=3, from=1-2, to=2-2]
            \arrow["{\mathcal{O}}", shift left=3,
                from=2-1, to=1-1]
            \arrow[shift left=3, from=2-2, to=1-2]
            \arrow[hook', from=2-2, to=2-1]
        \end{tikzcd}
        \]
    \end{column}

    \onslide<2->{
    \begin{column}{0.48\textwidth}
     \begin{center}
        \small{   \textbf{En objetos}
        \[
        \begin{aligned}
            S &\mapsto \mathcal{O}S,\\[1mm]
            A &\mapsto \pt A,
        \end{aligned}
        \]}
    \end{center}
    \end{column}
    }
\end{columns}
    
\vspace{0.15cm}

%------------------------------------------------
% Parte inferior
%------------------------------------------------
\begin{columns}[T,onlytextwidth]
\onslide<3->{
    \begin{column}{0.52\textwidth}
        \begin{center}
        \small{\textbf{En flechas}
        \[
        \begin{split}
        (f\colon S\to T)
        &\mapsto
        (f^{-1}\colon\mathcal{O}T\to\mathcal{O}S),\\
        (h\colon A\to B)
        &\mapsto
        (\pt(h)\colon\pt B\to\pt A).
        \end{split}
        \]}
        \end{center}
    \end{column}}
\onslide<4->{
    \begin{column}{0.44\textwidth}
        \begin{center}
        \small{\textbf{Reflexión espacial}
        \[
        \begin{split}
            \mathcal{U}_A
            &\colon
            A\to\mathcal{O}(\pt A),\\
            \mathcal{U}_A&(a)
            =\{p\in\pt A\mid a\nleq p\}.
        \end{split}
        \]}
        \end{center}
    \end{column}
}
\end{columns}
\onslide<5->{
\small{
Un marco \(A\) es \textbf{espacial} si sus puntos separan elementos:
\[
    a\nleq b
    \quad\Longrightarrow\quad
    \exists\,p\in\pt A:
    \quad b\leq p
    \quad\text{y}\quad
    a\nleq p.
\]}}
\footnotetext[0]{Ver \cite{Johnstone82, PicadoPultr, picado2021separation, simmonsbasics, simmonscollection, Zaldivar2023} para obtener información sobre marcos}
\end{frame}

\begin{frame}{Núcleos y filtros}

\begin{columns}[T,onlytextwidth]

%------------------------------------------------
% Columna izquierda: núcleos
%------------------------------------------------
\begin{column}{0.48\textwidth}
\small{
\(j:A\to A\) es un \textbf{núcleo} si
\begin{itemize}
\item $x \leq j(x)$,
\item $x\leq y \Rightarrow j(x)\leq j(y)$,
\item $j(x\wedge y)= j(x)\wedge j(y)$,
\item $j^2= j$.
\end{itemize}

\medskip
\onslide<2->{
\textbf{Ejemplos}

Para \(a\in A\),
\begin{itemize}
\item $u_a(x)=a\vee x$,
\item $v_a(x)=(a\succ x)$.
\end{itemize}
}}
\end{column}

%------------------------------------------------
% Columna derecha: filtros
%------------------------------------------------
\onslide<3->{
\begin{column}{0.48\textwidth}
\small{
\(F\subseteq A\) es un \textbf{filtro} si
\begin{itemize}
\item $1 \in F$,
\item $a,b\in F \Rightarrow a\wedge b\in F$,
\item $a\in F,\, a\leq b \Rightarrow b\in F$.
\end{itemize}
}

\medskip
\onslide<4->{
\textbf{Ejemplos}
\begin{itemize}
\item Filtro propio,
\item Filtro admisibile,
\item Filtro abierto.
\end{itemize}
}}
\end{column}

\end{columns}
\onslide<5->{
\small{
Existe una relación entre filtros y núcleos
\[
F\longleftrightarrow
v_F=\bigvee_{a\in F}v_a.
\]
denominamos a $v_F$ como el \textbf{núcleo ajustado} asociado a $F$.}}
\end{frame}

\section{La noción espacial}
\begin{frame}{Punto de partida}
    \begin{center}
\includegraphics[width=1\textwidth]{Imagenes/Sexton2006.png}
    \end{center}
     {\scriptsize
        R. A. Sexton and H. Simmons,
        \textit{An ordinal indexed hierarchy of separation properties},
        Topology Proceedings \textbf{30} (2006), 585--625.
        }
\end{frame}

\begin{frame}{Derivadas}

Sea \(S\) un espacio topológico.

Un operador
\[
\partial\colon \mathcal{C}S\longrightarrow \mathcal{C}S \qquad 
\]
es una \textbf{derivada} si:
\begin{itemize}
    \item $C\subseteq D\Rightarrow \partial(C)\subseteq \partial(D)$,
    \item $\partial(C)\subseteq C$,
    \item $\partial(C\cup D)=\partial(C)\cup\partial(D)$.
\end{itemize}

Si además
\[
\partial^2=\partial,
\]
decimos que \(\partial\) es una \textbf{derivada idempotente}.
\end{frame}

\begin{frame}{Núcleos espacialmente inducidos}

Sea \(S\) un espacio topológico y \(E\subseteq S\). Consideremos
\[
    [E]\colon\mathcal{O}S\longrightarrow\mathcal{O}S,
    \qquad
    [E](U)=(E\cup U)^\circ.
\]

El operador \([E]\) es un \textbf{núcleo espacialmente inducido}.

\vspace{0.3cm}

Su complemento dual es la derivada idempotente
\[
    \langle E\rangle\colon\mathcal{C}S\longrightarrow\mathcal{C}S, \qquad \langle E\rangle(X)=\overline{(E\cap X)}.
\]

Si $U=X'\in\mathcal{O}S$ (equivalentemente, \(X=U'\)), entonces
\[
    \boxed{
    \langle E\rangle(X)
    =([E](U))'
    =([E](X'))'.
    }
\]
\end{frame}

\begin{frame}{El teorema de Hofmann--Mislove}

Sea \(S\) un espacio topológico. Consideremos
\[
    Q\in\mathcal{Q}S
    \qquad\text{y}\qquad
    \nabla\in(\mathcal{O}S)^\wedge
\]
relacionados mediante el teorema de Hofmann--Mislove:
\[
    \nabla
    =
    \{V\in\mathcal{O}S\mid Q\subseteq V\}.
\]

Para \(X\in\mathcal{C}S\), definimos
\[
    \partial_Q(X)
    =
    \bigwedge_{Q\subseteq V}
    \overline{V\cap X}.
\]

Su complemento dual es el prenúcleo
\[
    f_\nabla(U)
    =
    \bigvee_{Q\subseteq V}
    (V'\cup U)^\circ,
    \qquad U\in\mathcal{O}S.
\]

\end{frame}

\begin{frame}{Iteración del prenúcleo}

El operador \(f_\nabla\) es un prenúcleo, pero no es
necesariamente idempotente.

Consideramos sus iteraciones transfinitas:
\[
    f_\nabla^0
    \leq
    f_\nabla
    \leq
    f_\nabla^2
    \leq
    \cdots
    \leq
    f_\nabla^\alpha
    \leq
    \cdots
\]

hasta alcanzar un ordinal para el cual la iteración se estabiliza.

Denotamos por
\[
    \boxed{f_\nabla^\infty}
\]
al núcleo obtenido mediante este proceso.

El operador \(f_\nabla^\infty\) es el
núcleo ajustado asociado a \(\nabla\).

\end{frame}

\begin{frame}{Espacios apilados}
\begin{block}{Definición (\cite{sexton2006ordinal}):}
Sea \(S\) un espacio topológico y \(Q\in\mathcal{Q}S\).

\vspace{0.3cm}

Decimos que \(Q\) es \textbf{apilado} si
\[
    \boxed{
        \overline{Q}=\partial_Q^\infty(S)
    }.
\]

Decimos que \(Q\) es \textbf{fuertemente apilado} si
\[
    \boxed{
        \langle Q\rangle=\partial_Q^\infty
    }.
\]

El espacio \(S\) es \textbf{apilado} (resp. \textbf{fuertemente
apilado}) si cada \(Q\in\mathcal{Q}S\) es apilado
(resp. fuertemente apilado).
\end{block}
\end{frame}

\begin{frame}{¿Por qué estudiar espacios apilados?}

Sexton y Simmons prueban las implicaciones
\[
    \text{eficiente}
    \Longrightarrow
    \text{fuertemente apilado}
    \Longrightarrow
    \text{apilado},
\]
y
\[
\text{eficiente}\iff \text{apilado y empaquetado.}
\]
Además,
\[
    \boxed{
    T_1+\text{apilado}
    \Longrightarrow
    \text{sobrio}.
    }
\]
\footnotetext[0]{En la literatura, podemos encontrar a la eficiencia como "\emph{tidy}" o "\emph{neat}".}
\footnotetext[0]{Estos y otros resultados en los que se involucran las propiedades de apilamiento pueden consultarse en \cite{sexton2003point,simmons2004vietoris,sexton2006point}.}
\end{frame}

\section{La noción en marcos}
\begin{frame}{¿Qué ocurre en marcos?}

En el caso espacial, la noción de apilamiento se construye a partir de

\[
\begin{array}{ccc}
Q\in\mathcal{Q}S
&\longleftrightarrow&
\nabla\in(\mathcal{O}S)^\wedge,
\\[3mm]
\langle Q\rangle
&&
f_\nabla^\infty.
\end{array}
\]

\vspace{0.4cm}

\begin{center}
\Large
¿Cómo trasladar esta construcción a un marco arbitrario \(A\)?
\end{center}

\end{frame}

\begin{frame}{Hofmann--Mislove--Johnstone}

En el caso espacial,
\[
    \nabla\in(\mathcal{O}S)^\wedge
    \quad\longmapsto\quad
    f_\nabla^\infty .
\]

Para un marco arbitrario \(A\), si \(F\in A^\wedge\),
el Teorema de Hofmann--Mislove--Johnstone determina
un intervalo
\[
    \boxed{
        [v_F,w_F]\subseteq NA
    }.
\]

Su extremo inferior
\[
    \boxed{v_F}
\]
es el núcleo ajustado asociado a \(F\).

\end{frame}

\begin{frame}{Pares de Hofmann--Mislove}

Consideremos un filtro abierto de Scott
\[
    F\in A^\wedge
\]
y un compacto saturado
\[
    Q\in\mathcal{Q}S.
\]

Cuando \(F\) y \(Q\) están relacionados mediante la
correspondencia de Hofmann--Mislove, diremos que
\[
    \boxed{(F,Q)}
\]
es un \textbf{par HM}.

\vspace{0.4cm}

Estos objetos determinan, respectivamente,
\[
    \boxed{v_F}
    \qquad\text{y}\qquad
    \boxed{\mathcal{U}_*\circ[Q']\circ\mathcal{U}}.
\]

\end{frame}

\begin{frame}{Intervalos de admisibilidad}

Sea \((F,Q)\) un par HM y
\(\nabla=\nabla(Q)\). Si
\[
    k\in[v_\nabla,w_\nabla],
\]
entonces
\[
    \boxed{
    \mathcal{U}_*\circ k\circ\mathcal{U}
    \in[v_F,w_F].
    }
\]

Por tanto, obtenemos una aplicación
\[
    [v_\nabla,w_\nabla]
    \longrightarrow
    [v_F,w_F],
    \qquad
    k\longmapsto
    \mathcal{U}_*\circ k\circ\mathcal{U}.
\]

\end{frame}

\begin{frame}{Comportamiento de los extremos}

La aplicación
\[
    k\longmapsto
    \mathcal{U}_*\circ k\circ\mathcal{U}
\]
no preserva, en general, ambos extremos del intervalo.

\vspace{0.4cm}

Para el extremo inferior,
\[
    \boxed{
    v_F
    \leq
    \mathcal{U}_*\circ v_\nabla\circ\mathcal{U}
    }.
\]

\vspace{0.5cm}

En cambio, para el extremo superior,
\[
    \boxed{
    w_F
    =
    \mathcal{U}_*\circ w_\nabla\circ\mathcal{U}
    }.
\]

\end{frame}

\begin{frame}{El caso \(T_1\)}

Si el espacio \(S\) es \(T_1\), entonces
\[
    [Q']=w_\nabla.
\]

Por tanto,
\[
    w_F
    =
    \mathcal{U}_*\circ w_\nabla\circ\mathcal{U}
    =
    \mathcal{U}_*\circ[Q']\circ\mathcal{U}.
\]

En consecuencia,
\[
    \boxed{
    v_F
    \leq
    \mathcal{U}_*\circ[Q']\circ\mathcal{U}
    =
    w_F.
    }
\]
\footnotetext[0]{Recordemos que un marco $A$ es $T_1$ si para todo $p\in \pt A$, $p$ es máximo.\\}
\end{frame}

\begin{frame}{Marcos apilados}

\begin{block}{Definición:}
Sea \(A\) un marco y \((F,Q)\) un par HM.

Decimos que \(A\) es \textbf{apilado} si
\[
    \boxed{
    v_F(0)
    =
    \bigl(
        \mathcal{U}_*\circ[Q']\circ\mathcal{U}
    \bigr)(0)
    }
\]
para todo par HM \((F,Q)\).

\vspace{0.5cm}

Decimos que \(A\) es \textbf{fuertemente apilado} si
\[
    \boxed{
    v_F
    =
    \mathcal{U}_*\circ[Q']\circ\mathcal{U}
    }
\]
para todo par HM \((F,Q)\).
\end{block}
\end{frame}

\begin{frame}{Del espacio al marco}

\begin{center}
\begin{tabular}{c@{\qquad\qquad}c}
    \textbf{Espacios} & \textbf{Marcos} \\[0.3cm]

    \((\nabla,Q)\) par HM
    &
    \((F,Q)\) par HM
    \\[0.5cm]

    \textbf{Apilado}
    &
    \textbf{Apilado}
    \\[0.2cm]

    \(
        \overline{Q}=\partial_Q^\infty(S)
    \)
    &
    \(
        v_F(0)
        =
        \bigl(
        \mathcal{U}_*\circ[Q']\circ\mathcal{U}
        \bigr)(0)
    \)
    \\[0.7cm]

    \textbf{Fuertemente apilado}
    &
    \textbf{Fuertemente apilado}
    \\[0.2cm]

    \(
        [Q']=f_\nabla^\infty
    \)
    &
    \(
        v_F
        =
        \mathcal{U}_*\circ[Q']\circ\mathcal{U}
    \)
\end{tabular}
\end{center}

\end{frame}

\begin{frame}
\begin{block}{Proposición:}
Las nociones de \textbf{apilado} y
\textbf{fuertemente apilado} son conservativas.
\end{block}

Consideremos \(A=\mathcal{O}S\) y \((F=\nabla,Q)\) un par HM.

\vspace{0.2cm}

Si \(S\) es \textbf{fuertemente apilado}, entonces
\[
    v_F=[Q']
\iff    \mathcal{U}_*\circ[Q']\circ\mathcal{U}
    =[Q']=v_F.
\]

Si \(S\) es \textbf{apilado}, entonces
\[
    \overline{Q}=\partial_Q^\infty(S)\iff  (\mathcal{U}_*\circ [Q']\circ \mathcal{U})(\emptyset)=(Q')^\circ=v_\nabla(\emptyset) 
\]
\end{frame}

\begin{frame}{Ejemplos}

\begin{block}{Ejemplo 1}
Los \textbf{espacios de Alexandroff} son fuertemente apilados.

Por conservatividad,
\[
    S \text{ Alexandroff}
    \quad\Longrightarrow\quad
    \mathcal{O}S \text{ es fuertemente apilado}.
\]
\end{block}

\begin{block}{Ejemplo 2:}
Consideremos el marco $A=[0,1]$, entonces
\[
S=\pt A=[0,1)
\]
es fuertemente apilado, pues $v_F=[Q']$ para todo par HM.
\end{block}

\alert{En 1), si S es discreto, $\mathcal{O}S$ es $T_1$. En 2), $A$ no es $T_1$.}
\end{frame}

\begin{frame}{Del caso espacial al caso de marcos}

Para espacios, Sexton--Simmons prueban que
\[
    \boxed{
    S\text{ es }T_1\text{ y apilado}
    \quad\Longrightarrow\quad
    S\text{ es sobrio}.
    }
\]

\vspace{0.5cm}

Al trasladar este fenómeno al contexto de marcos,
la pregunta natural es:

\[
    \boxed{
    A\text{ es }T_1\text{ y apilado}
    \quad\stackrel{?}{\Longrightarrow}\quad
    A\text{ es espacial}.
    }
\]

\vspace{0.5cm}

Nuestros resultados muestran que varias consecuencias
del caso espacial permanecen válidas
\textbf{sin suponer que \(A\) sea espacial}.

\end{frame}

\begin{frame}{Colapso del intervalo de admisibilidad}

\begin{block}{Proposición:}
Sea \(A\) un marco y \((F,Q)\) un par HM.

\begin{enumerate}
    \item Si \(A\) es \(T_1\) y apilado, entonces
    \[
        \boxed{v_F(0)=w_F(0)}.
    \]

    \item Si
    \[
        v_F(0)=w_F(0),
    \]
    entonces \(A\) es apilado.
\end{enumerate}
\end{block}

En particular, con $a\neq 1$, si 
\[
    v_F(0)=w_F(0)\quad \text{y}\quad \mathcal{V}(a)=\{q\in\operatorname{pt}A\mid a\leq q\}\neq \emptyset,
\]
entonces $A$ es $T_1$.
\end{frame}

\begin{frame}{El espacio de puntos de \(VA\)}

Los puntos de \(VA\) están descritos por
\[
    \operatorname{pt}(VA)
    =
    \left\{
        (F,a)\mid
        F\in A^\wedge,\;
        v_F(0)\leq a\leq w_F(0)
    \right\}.
\]

Consideremos la proyección
\[
    \pi\colon \operatorname{pt}(VA)\longrightarrow A^\wedge,
    \qquad
    (F,a)\longmapsto F.
\]

Si \(A\) es \(T_1\) y apilado, entonces
\[
    v_F(0)=w_F(0)
\]
para todo \(F\in A^\wedge\).

Por tanto, cada fibra de \(\pi\) contiene un único elemento.

\end{frame}

\begin{frame}{Una consecuencia del colapso}

Consideremos
\[
    \pi\colon\operatorname{pt}(VA)\longrightarrow A^\wedge.
\]

\begin{block}{Proposición:}
Se tienen las siguientes implicaciones:

\[
\begin{aligned}
A\text{ es }T_1\text{ y apilado}
&\Longrightarrow
\mathcal{O}(A^\wedge)
\cong
\mathcal{O}(\operatorname{pt}(VA)),
\\[0.4cm]
\mathcal{O}(\pi)\text{ es un isomorfismo}
&\Longrightarrow
A\text{ es apilado}.
\end{aligned}
\]

Además, bajo la condición
\[
    \mathcal{V}(a)\neq\varnothing,
\]
con $a\neq 1$, el isomorfismo anterior implica que \(A\) es \(T_1\).
\end{block}

\end{frame}

\begin{frame}{Cocientes}

\begin{block}{Proposición:}
Sea \(A\) un marco y \(j\in NA\). Si \(A\) es
\textbf{fuertemente apilado}, entonces el cociente
\[
    A_j
\]
es fuertemente apilado.
\end{block}

\vspace{0.5cm}

En particular, la clase de marcos fuertemente apilados
es cerrada bajo cocientes.

\end{frame}

\begin{frame}{Una pregunta natural}

En espacios se tiene
\[
    T_1+\text{apilado}
    \quad\Longrightarrow\quad
    \text{sobrio}.
\]

\vspace{0.5cm}

En marcos hemos obtenido
\[
    T_1+\text{apilado}
    \quad\Longrightarrow\quad
    v_F(0)=w_F(0),
\]
y, consecuentemente,
\[
    \mathcal{O}(A^\wedge)
    \cong
    \mathcal{O}(\operatorname{pt}(VA)).
\]

\vfill

\begin{center}
\Large
\textbf{¿Existen marcos \(T_1\) apilados\\
que no sean espaciales?}
\end{center}

\vfill

\end{frame}

\begin{frame}{Una posible familia de ejemplos}

Sabemos que
\[
    \mathbf{(H)}\quad\Longrightarrow\quad T_1.
\]

Actualmente estudiamos si también se tiene
\[
    \boxed{
    \mathbf{(H)}\quad\Longrightarrow\quad\text{apilado}.
    }
\]

En \cite{Paseka1987} presentan una clase de marcos que satisfacen $\mathbf{(H)}$ que no son espaciales.

\footnotetext[0]{Decimos que un marco $A$ satisface la propiedad $\mathbf{(H)}$ si para cualesquier $1\neq a\nleq b\in A$ existe $c\in A$ tal que
\[
c\nleq a\quad\text{y}\quad \neg c\nleq b 
\]}
\end{frame}

\section{Una posible generalización}

\begin{frame}[fragile]{La idea detrás de la definición}

\[
\begin{tikzcd}[column sep=large, row sep=large]
    A && NA & {[v_F,w_F]} \\
    & \longmapsto && \dashv \\
    {\mathcal{O}S} && {N\mathcal{O}S} & {[v_\nabla,w_\nabla]}
    \arrow["{\mathcal{U}_A}"', from=1-1, to=3-1]
    \arrow[squiggly, maps to, from=1-3, to=1-4]
    \arrow["{N(\mathcal{U}_A)}"', from=1-3, to=3-3]
    \arrow[shift right=5, from=1-4, to=3-4]
    \arrow[squiggly, maps to, from=3-3, to=3-4]
    \arrow["{N(\mathcal{U}_A)_*}"', shift right=5, from=3-4, to=1-4]
\end{tikzcd}
\]

\vspace{0.4cm}

La definición compara operadores sobre \(A\) que provienen de
\textbf{construcciones distintas}:
\[
    \boxed{
    v_F
    \qquad\text{y}\qquad
    \mathcal{U}_*\circ[Q']\circ\mathcal{U}.
    }
\]

\end{frame}

\begin{frame}[fragile]{Hacia una noción más general}

\[\begin{tikzcd}
	A && NA & {[v_F,\tp_A]} \\
	& \longmapsto && \dashv \\
	B && NB & {[v_{f[F]}, \tp_B]}
	\arrow["f"', from=1-1, to=3-1]
	\arrow[squiggly, maps to, from=1-3, to=1-4]
	\arrow["{N(f)}"', from=1-3, to=3-3]
	\arrow[shift right=5, from=1-4, to=3-4]
	\arrow[squiggly, from=3-3, to=3-4]
	\arrow[shift right=5, from=3-4, to=1-4]
\end{tikzcd}\]

\vspace{0.4cm}

La pregunta es qué condiciones permiten comparar,
mediante \(N(f)\) y su adjunto derecho, núcleos adecuados de
\[
    [v_F,\tp_A]
    \qquad\text{y}\qquad
    [v_{f[F]},\tp_B].
\]

\end{frame}

\begin{frame}{Conclusiones}

\begin{itemize}

    \item Las nociones espaciales de apilado y fuertemente apilado
    admiten una extensión \textbf{conservativa} al contexto de marcos.

    \vspace{0.3cm}

    \item La interacción entre \(T_1\), apilamiento y espacialidad
    plantea nuevas preguntas genuinamente \emph{point-free}.

    \vspace{0.3cm}

    \item La construcción sugiere una noción más general,
    asociada a un morfismo arbitrario de marcos
    \[
        f\colon A\longrightarrow B.
    \]

\end{itemize}
\end{frame}

\End

\section*{\textsc{Referencias}}
\begin{frame}[allowframebreaks]
\bibliographystyle{plainnat}
%\bibliographystyle{plain}
%\bibliographystyle{amsalpha}
\bibliography{research2}
\end{frame}

\begin{frame}[plain,standout]
      \centering
      \Huge{\bfseries \smiley¡Muchas gracias!\smiley}
      %In combination with \textit{plain}, \\
      %it makes a nice thank-you slide!
      %\scalebox{4}{\faGithub} \par\bigskip
      %\url{https://github.com/JCmonter/Apuntes/tree/main/Presentaciones} \\
      %\url{https://ctan.org/pkg/beamertheme-arguelles}
\end{frame}
\end{document}